{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main photoforge window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
